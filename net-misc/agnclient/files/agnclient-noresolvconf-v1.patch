diff -ur clean/opt/agns/bin/ConfigAGN.sh dirty/opt/agns/bin/ConfigAGN.sh
--- clean/opt/agns/bin/ConfigAGN.sh	2007-12-13 14:48:30.000000000 -0500
+++ dirty/opt/agns/bin/ConfigAGN.sh	2007-12-13 16:38:44.000000000 -0500
@@ -149,65 +149,11 @@
 }
 
 write_resolvconf() {
-	grep '^#@NETVPN_GENERATED@' /etc/resolv.conf > /dev/null 2>&1 || \
-		cp -- /etc/resolv.conf "$RESOLV_CONF_BACKUP"
-	NEW_RESOLVCONF="#@NETVPN_GENERATED@ -- this file is generated by NetVPN
-# and will be overwritten by NetVPN
-# as long as the above mark is intact"
-
-	while [ -n "$INTERNAL_IP4_DNS" ]; do
-		read ONE_NAMESERVER INTERNAL_IP4_DNS <<-EOF
-	$INTERNAL_IP4_DNS
-EOF
-		LINE="nameserver $ONE_NAMESERVER"
-		NEW_RESOLVCONF="$NEW_RESOLVCONF
-$LINE"
-	done
-	
-	exec 6< /etc/resolv.conf
-	while read LINE <&6 ; do
-		case "$LINE" in
-#			nameserver*)
-#				if [ -n "$INTERNAL_IP4_DNS" ]; then
-#					read ONE_NAMESERVER INTERNAL_IP4_DNS <<-EOF
-#	$INTERNAL_IP4_DNS
-# EOF
-#					LINE="nameserver $ONE_NAMESERVER"
-#				else
-#					LINE=""
-#				fi
-#				;;
-			domain*|search*)
-				if [ -n "$NETVPN_DEF_DOMAIN" ]; then
-					LINE="$LINE $NETVPN_DEF_DOMAIN"
-					NETVPN_DEF_DOMAIN=""
-				fi
-				;;
-		esac
-		NEW_RESOLVCONF="$NEW_RESOLVCONF
-$LINE"
-	done
-	exec 6<&-
-	
-	for i in $INTERNAL_IP4_DNS ; do
-		NEW_RESOLVCONF="$NEW_RESOLVCONF
-nameserver $i"
-	done
-	if [ -n "$NETVPN_DEF_DOMAIN" ]; then
-		NEW_RESOLVCONF="$NEW_RESOLVCONF
-search $NETVPN_DEF_DOMAIN"
-	fi
-	echo "$NEW_RESOLVCONF" > /etc/resolv.conf
-	cp -- /etc/resolv.conf "$NEW_RESOLV_CONF_BACKUP"
+	return
 }
 
 reset_resolvconf() {
-	if [ ! -e "$RESOLV_CONF_BACKUP" ]; then
-		return
-	fi
-	grep '^#@NETVPN_GENERATED@' /etc/resolv.conf > /dev/null 2>&1 && cat "$RESOLV_CONF_BACKUP" > /etc/resolv.conf
-	rm -f -- "$RESOLV_CONF_BACKUP"
-	rm -f -- "$NEW_RESOLV_CONF_BACKUP"
+	return
 }
 
 do_connect() {
